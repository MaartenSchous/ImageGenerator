# ImageGenerator
This PHP website allows a visitor to enter an image creation prompt. A C# evaluation tool facilitates aproval and uploads to the gallery. It’s built with PHP, SQL and C# .net. It uses a local AI that runs in Docker to generate the images.
